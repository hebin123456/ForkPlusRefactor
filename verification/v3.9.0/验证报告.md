# ForkPlus v3.9.0 功能验证报告（Ubuntu / Wine 环境）

> 验证时间：2026-08-09 · 环境：Ubuntu 22.04 沙箱（无图形界面）+ Wine 11 + Xvfb + 手工拼装的 .NET 10 WindowsDesktop 运行时

## 1. 结论：原版客户端可在纯 Linux 环境完整运行

**ForkPlus v3.9.0 的 Windows 发行版已在纯 Linux 沙箱中成功启动并跑通完整 Git 工作流**，覆盖启动、打开仓库、提交图浏览、Diff/文件树、各类右键上下文菜单、本地改动暂存、提交 / Pull / Push、Undo / Redo、新建分支 / 标签、子模块、Open With、控制台、Work 下拉等。共 **51 张截图**，见同目录 `verification/v3.9.0/`（或本目录下的 `index.html` 索引）。

这为本工程迁移到 Avalonia 提供了「随时可启动、可截图、可自动化对比」的**标准答案基准**——不需要 Windows 真机也能逐页做像素级 / 行为级回归。

## 2. 相对 v3.8.3 的变化（来自 v3.8.3 → v3.9.0 提交记录，共 3 个 commit）

| Commit | 说明 |
|--------|------|
| `refactor(v3.9.0)` | 统一所有 AI 辅助界面的设计与实现 |
| `fix(v3.9.0)` | 修复 AI 窗口 XAML 根元素与基类不一致导致的编译错误 |
| `fix(v3.9.0)` | 修复 AiStreamingWebView 缺失 `WebView2EnvironmentHelper` using 导致的编译错误 |

> 即 **v3.9.0 的核心改动是 AI 辅助界面（WebView2 承载的流式 Markdown 渲染）的统一重构**。这与 Avalonia 迁移中工作量最大、风险最高的「WebView2 替换」点直接相关——迁移时应把 AI 流式界面作为重点对照区域。

## 3. 验证环境搭建（可复现）

```
1. Wine 11.0（winehq-stable 官方源）+ Xvfb 虚拟显示 :99
2. .NET 10 WindowsDesktop 运行时：从 NuGet 拉取三个运行时包
   (Microsoft.NETCore.App / WindowsDesktop.App Runtime + Host win-x64)
   手工拼装 shared framework 目录结构，用 DOTNET_ROOT 环境变量指入
3. PortableGit 解压到 C:\PortableGit（应用自动探测成功）
4. 字体：用 fontforge 将 Droid Sans Fallback 改名伪装为
   "Microsoft YaHei UI" / "Segoe UI" / "Consolas" 等字体族，
   放入 C:\windows\Fonts 并写入注册表 Fonts 键
```

## 4. 原版程序的两处崩溃点（已用 IL 补丁绕过）

原版 `ForkPlus.dll` 在 Wine 下有两处必崩，均为 **Wine 未实现的 WinRT API**（真 Windows 上完全正常），v3.9.0 中依然存在：

| # | 崩溃点 | 原因 | 补丁方式 |
|---|--------|------|---------|
| 1 | `SystemThemeHelper.SubscribeToSystemEvents()` | `UISettings.ColorValuesChanged` 事件订阅在 Wine 返回 E_NOTIMPL | 方法体替换为 `ret` |
| 2 | `SystemThemeHelper.GetSystemBrush()` | 依赖 UISettings 取系统强调色，返回空引用 | 方法体替换为返回 `null`（上层有 fallback 画笔） |

补丁工具：Mono.Cecil（`patcher.cs`），只跳过系统主题感知，应用自带主题不受影响（深色主题切换已验证）。

## 5. 截图功能映射（51 张，按功能分组）

### 启动与基础
| 截图 | 内容 |
|------|------|
| `01-welcome-v390.png` | 启动 / 欢迎界面（v3.9.0） |
| `06-after-restart.png` | 重启应用后的主界面状态 |
| `07-tooltip-check.png` | 控件 tooltip 渲染检查 |

### 打开仓库 / 外部打开
| 截图 | 内容 |
|------|------|
| `02-open-dialog.png` | 打开仓库对话框 |
| `v2-37-openwith.png` | Open With：用外部程序打开文件 |

### 提交图 / 历史 / Diff / 文件树
| 截图 | 内容 |
|------|------|
| `03-demo-graph.png` | 提交图（多分支 / 合并提交演示仓库） |
| `04-diff-tab.png` | Diff 标签页（语法高亮、等宽字体正常） |
| `05-filetree-tab.png` | 文件树标签页 |

### 提交右键上下文菜单
| 截图 | 内容 |
|------|------|
| `08-commit-ctx.png` | 提交右键菜单（一级） |
| `08-commit-ctx_000.png` | 提交右键子菜单展开 |
| `v2-11-commit-ctx.png` | 提交上下文菜单（含更多操作项） |
| `v2-19-commit-msg.png` | 提交信息输入框 |
| `v2-20-after-commit.png` / `v2-20b-after-commit.png` | 提交后状态 |

### 分支右键上下文菜单 / 分支操作
| 截图 | 内容 |
|------|------|
| `09-branch-ctx.png` | 分支右键菜单（一级） |
| `09-branch-ctx_000.png` / `_001.png` / `_002.png` | 分支菜单多级展开 |
| `v2-29-new-branch.png` / `v2-30-new-branch.png` / `v2-31-new-branch-filled.png` | 新建分支对话框（空 / 输入 / 已填） |
| `v2-32-branch-created.png` / `v2-33-branch-created.png` | 分支创建结果 |
| `v2-39-work-dropdown.png` | Work 下拉（分支切换） |

### 标签
| 截图 | 内容 |
|------|------|
| `v2-12-tags.png` | 标签列表 |
| `v2-34-new-tag.png` | 新建标签对话框 |
| `v2-35-tag-created.png` / `v2-36-tag-done.png` | 标签创建结果 |

### Stash
| 截图 | 内容 |
|------|------|
| `10-stash-ctx.png` / `v2-10-stash-ctx.png` | Stash 右键菜单 |
| `v2-28-stash-dropdown.png` | Stash 下拉列表 |

### 侧边栏 / 本地改动 / 暂存 / 子模块
| 截图 | 内容 |
|------|------|
| `v2-14-sidebar-expanded.png` | 侧边栏展开态 |
| `v2-16-sidebar-full.png` | 侧边栏完整内容 |
| `v2-13-submodule.png` | 子模块视图 |
| `v2-15-origin-submodule.png` | origin 子模块 |
| `v2-17-local-changes.png` | 本地改动（未暂存） |
| `v2-18-staged.png` / `v2-18b-staged.png` | 暂存区（已 stage） |
| `v2-29b-recheck.png` | 重新检查改动 |

### Pull / Push
| 截图 | 内容 |
|------|------|
| `v2-21-pull-dialog.png` / `v2-21-after-pull.png` / `v2-22-pull-done.png` | Pull 对话框与完成态 |
| `v2-25-push-dialog.png` / `v2-26-push-dialog.png` / `v2-27-push-done.png` | Push 对话框与完成态 |

### Undo / Redo
| 截图 | 内容 |
|------|------|
| `v2-23-undo-confirm.png` / `v2-23-after-undo.png` | Undo 确认与结果 |
| `v2-24-undo-result.png` / `v2-24-after-redo.png` / `v2-25-redo-result.png` | Redo 结果与状态 |

### 控制台
| 截图 | 内容 |
|------|------|
| `v2-38-console.png` | 内置控制台（git 命令输出 / 日志） |

## 6. 构造的 Git 场景（用于覆盖上述功能）

验证用演示仓库由 `make_fixture.sh` 生成，刻意构造了可触发各 UI 路径的数据：

- 多分支 + 合并提交（驱动提交图的多条线与合并节点）
- 带注释 / 轻量标签（驱动标签列表与新建标签）
- Stash 条目（驱动 Stash 菜单与下拉）
- 本地未提交改动 + 已暂存文件（驱动本地改动 / 暂存区 / Recheck）
- bare 远程仓库（驱动 Pull / Push 对话框与完成态）
- 嵌套子模块（驱动子模块视图）
- Open With 关联程序（驱动外部打开）

## 7. 对 Avalonia 迁移的对照价值

1. **基准对照通道已打通**：Wine 环境可随时启动原版截图，v3.9.0 已覆盖 51 个界面 / 交互点，迁移过程中每个页面都能拿到标准答案做对比，不依赖 Windows 真机。
2. **交互可自动化验证**：xdotool 能驱动原版走完整流程，迁移后用 `Avalonia.Headless` 做同样操作，行为级回归可行。
3. **风险集中在三点**（与 v3.8.3 结论一致，且 v3.9.0 让 AI 界面区域更聚焦）：
   - **WebView2 替换**：v3.9.0 统一了 AI 流式界面，是迁移重点，需选 `WebView.Avalonia` / `CefGlue` 方案；
   - **Toast / 系统主题的平台抽象**：Wine 下已验证仅两处 WinRT 调用需平台适配；
   - **XAML 逐页翻译**：WPF→Avalonia 是 XAML 技术栈里迁移成本最低的路径，AI 界面统一后结构更清晰。
4. **其余基本是平移**：Rust 引擎（`biturbo.dll`）、`tokei.exe` 代码统计、git 子进程调用、MVVM 业务层，跨平台无压力。

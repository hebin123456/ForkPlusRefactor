<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ForkPlus.Avalonia</RootNamespace>
    <AssemblyName>ForkPlus.Avalonia</AssemblyName>
    <!-- XamlX IL 编译（EnableAvaloniaXamlCompilation=true）是 Avalonia 12 生成 InitializeComponent
         的唯一途径（AvaloniaXamlLoader.Load 在 12.1.1 已改为抛异常）。该编译由 Csc 通过
         TargetsTriggeredByCompilation 触发 CompileAvaloniaXaml 目标完成。 -->
    <EnableAvaloniaXamlCompilation>true</EnableAvaloniaXamlCompilation>
    <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
    <!-- 覆盖 CoreCompile 以透传 TargetsTriggeredByCompilation（详见 OverrideCoreCompile.targets）。
         必须用 CustomAfterMicrosoftCommonTargets，因为 SDK 的 CoreCompile 在项目内容之后导入，
         直接在 csproj 里重定义会被 SDK 版本覆盖（后定义优先）。 -->
    <CustomAfterMicrosoftCommonTargets>$(MSBuildThisFileDirectory)OverrideCoreCompile.targets</CustomAfterMicrosoftCommonTargets>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="12.1.1" />
    <PackageReference Include="Avalonia.Desktop" Version="12.1.1" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="12.1.1" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="12.1.1" />
  </ItemGroup>

</Project>
